#pragma once

/* =========================================================
 *
 * Used to identify the configuration of the game
 * 
 * Options include
 * 1. 2-player ( default )
 * 2. 4-player
 * 
 * ========================================================= */

enum class GameConfiguration
{
    TWO_PLAYER = 0,
    FOUR_PLAYER
};